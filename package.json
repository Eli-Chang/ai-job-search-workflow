{
  "name": "ai-job-search-workflow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Synthetic reference implementation for evidence-backed job-search workflow controls.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "npm test"
  }
}
